{
  "name": "ai-audit-tool",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd app && npx vite build && mkdir -p ../public && cp -r dist/* ../public/",
    "vercel-build": "cd app && npx vite build && mkdir -p ../public && cp -r dist/* ../public/"
  },
  "dependencies": {
    "openai": "^4.70.0"
  }
}
