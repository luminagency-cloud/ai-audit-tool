{
  "name": "ai-audit-tool",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "vercel-build": "npx vite build && mkdir -p public && cp -r app/dist/* public/"
  },
  "dependencies": {
    "openai": "^4.70.0"
  },
  "devDependencies": {
    "vite": "^8.0.12",
    "@vitejs/plugin-react": "^6.0.1"
  }
}
